applications:
  - name: flight-details
    memory: 2G
    disk_quota: 4G
    buildpacks:
      - nodejs_buildpack
    command: >
      sh -c "npm run dev"
    env:
      NPM_CONFIG_LEGACY_PEER_DEPS: "true"
      NODE_ENV: production